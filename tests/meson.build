# common dependency for every test suite
testdrive_dep = dependency('test-drive', fallback: ['test-drive', 'testdrive_dep']) 

# every chapter has its own independent test suite
subdir('ch1')
subdir('ch2')

